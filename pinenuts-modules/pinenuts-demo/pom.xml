<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.pinenuts.boot</groupId>
        <artifactId>pinenuts-modules</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>pinenuts-demo</artifactId>

    <description>
        demo模块
    </description>

    <dependencies>

        <!-- 通用工具-->
        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-core</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-doc</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-sms</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-mail</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-redis</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-idempotent</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-mybatis</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-log</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-excel</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-security</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-web</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-ratelimiter</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-translation</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-sensitive</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-encrypt</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-tenant</artifactId>
        </dependency>

        <dependency>
            <groupId>com.pinenuts.boot</groupId>
            <artifactId>pinenuts-common-websocket</artifactId>
        </dependency>

    </dependencies>

</project>
